#spring:
#  datasource:
#    url: jdbc:postgresql://127.0.0.1:5432/codify
#    username: postgres
#    password: new_password
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#    show-sql: true

server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/School
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update